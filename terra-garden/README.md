# The Garden — bracelet + web build

Front end: the Garden page from `tianlinsy/tterra_teest` (Daniel's page), now backed by Firestore.
Hardware: Seeed XIAO ESP32-C3 + 9× WS2812B + 3.7 V LiPo, one LED per connection.

Start with **SETUP_GUIDE.md**. Files you edit: `web/firebase-config.js` and `firmware/garden_bracelet/config.h`.
